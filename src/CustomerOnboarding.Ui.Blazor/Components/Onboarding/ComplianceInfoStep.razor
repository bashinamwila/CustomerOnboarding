@rendermode InteractiveServer
@using CustomerOnboarding.Ui.Blazor.Components.Onboarding
@inject Csla.Blazor.State.StateManager StateManager

@if(ViewModel.Model is not null)
{
    <div class="container mx-auto px-4 py-8 max-w-6xl">
        <div class="p-6 sm:p-8">

            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 bg-white shadow-sm">
                <div class="flex items-center justify-start h-16">
                    <!-- Bank Icon (Using Heroicons 'building-library') -->
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-7 h-7 text-indigo-600 mr-3">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 21v-8.25M15.75 21v-8.25M8.25 21v-8.25M3 9l9-6 9 6m-1.5 12V10.332A48.36 48.36 0 0012 9.75c-2.551 0-5.056.2-7.5.582V21M3 21h18M12 6.75h.008v.008H12V6.75z" />
                    </svg>
                    <h1 class="text-xl font-semibold text-gray-800"> Compliance Information</h1>
                </div>
            </div>
            <!-- Section 1: Information -->
            <GeneralComplianceInformationStep IsVisible=@(((CustomerOnboarding.BusinessLibrary.ComplianceInfoStep)ViewModel.Model.Steps[2]).CurrentStepIndex==0)
            ViewModel="ViewModel" />
            <StatutoryRegistrations IsVisible=@(((CustomerOnboarding.BusinessLibrary.ComplianceInfoStep)ViewModel.Model.Steps[2]).CurrentStepIndex==1)/>
        </div>
    </div>
}

@code {
    [Parameter]
    public ViewModel<TenantOnboardingOrchestrator> ViewModel { get; set; } = default!;

    private void HandleViewModelChanged(ViewModel<TenantOnboardingOrchestrator> vm) => ViewModel = vm;

    protected override async Task OnInitializedAsync()
    {
        await StateManager.InitializeAsync();
        
    }
}
